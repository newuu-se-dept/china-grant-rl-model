gymnasium==1.1.1
tianshou==0.5.1
numpy>=1.24
pandas>=2.0

# PyTorch — choose ONE depending on your machine:
#   Mac (CPU only):
#     pip install torch
#   DGX Spark / Linux with CUDA 12.8 (GB10 Blackwell):
#     pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.0
